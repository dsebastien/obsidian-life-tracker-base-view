# 2026-07-30

## Custom filename date patterns (issue #139)

Filename date parsing is now user-configurable. Notes named anything other than
the five built-in ISO formats (`YYYY-MM-DD`, `YYYY-Www`, `YYYY-MM`, `YYYY-Qq`,
`YYYY`) previously resolved no date anchor.

- New `src/utils/filename-date.utils.ts`: `{{token}}` pattern compiler
  (`{{date}}`, `{{year}}`, `{{month}}`, `{{day}}`, `{{week}}`, `{{quarter}}`,
  plus `*` wildcards), validator, example renderer, the built-in patterns, the
  configured-pattern registry, `parseDateFromFilename` and `getDateFromISOWeek`.
  Everything filename-related moved out of `date.utils.ts` so the dependency
  stays one-way; the moved tests moved with it.
- `filenameDatePatterns` setting (`{ id, pattern }[]`, default `[]`), compiled
  into the registry by `plugin.applyDateSettings()` on load and on every
  settings update (`applyWeekStart` renamed to cover both concerns).
- New "Dates" settings tab (`date-settings-section.ts`) with the pattern list
  editor: per-pattern validation error or live matching example, plus a token
  reference. "First day of the week" moved there from "Visualizations".
- "Capture today" benefits automatically (it filters on daily granularity).

## Decisions

- Token vocabulary copied from `obsidian-starter-kit-plugin`'s
  `expression-evaluator.ts` so the syntax is the same across the plugin family.
  `obsidian-ai-editor` has no equivalent mechanism.
- Granularity is inferred from the token set rather than configured — one less
  thing to get wrong, and it cannot contradict the pattern.
- Custom patterns are tried before the built-ins, which always remain as a
  fallback: no migration, no breakage for existing vaults.
- `*` chosen as the wildcard because it cannot appear in a filename, so it is
  unambiguous. Everything else in a pattern is regex-escaped.
- Month names (`January`) deliberately not supported: locale handling would be
  half-broken for non-English vaults. Numeric tokens only.

## Open questions

- Should patterns be matched against the full path (folder + name) as well as
  the basename? Currently basename only, like the built-ins.
- Needs manual runtime verification in a vault (settings UI states, a matching
  note landing on the right heatmap cell, "Capture today").

## Vault audit (dsebastien's vault)

Checked `~/notesSeb` for #139: **no custom patterns needed**. Every periodic
note already uses a built-in format (2083 files: daily `2026-07-23`, weekly
`2026-W30`, monthly `2026-07`, quarterly `2026-Q3`, yearly `2026`); zero
deviations. Daily notes live under `40 Journal/41 Daily Notes/{{year}}/{{week}}`
but only the basename is parsed, so the folder shape is irrelevant.

One real config smell found: `Health (Base).base`'s Life Tracker view sets
`dateAnchorProperty: note.created`, but the daily notes carry `date`, not
`created` — the anchor silently falls through to the filename (which works).
Recommend pointing it at `note.date` or clearing it.

## Batch: #104, #116, #123, #125, #135, #140

- **#116** — already fixed by ec01915; verified all five bullets and closed.
- **#125** — number editor gains −/+ buttons (not in compact grid cells). Step
  math extracted to `number-step.utils.ts` (pure + tested): configured step or
  1, clamped to range, 6-decimal rounding, empty-field rules (range min when
  bounded, +step / 0 when not). Buttons commit immediately and disable at bounds.
  `injectDefaultButton` now moves the whole stepper, not just the input.
- **#140** — swipe navigation in the capture modal via pointer events, touch/pen
  only. Recognition extracted to `swipe.utils.ts` (pure + tested): ≥60px,
  ≥1.5× vertical, ≤800ms. Gestures starting on interactive controls are ignored;
  boundary actions stay button-only. Mobile touch targets bumped to 44px
  (property nav), 40px (file nav) — they had _shrunk_ to 32/24px on mobile.
- **#104** — heatmap: one delegated listener set on the grid root instead of six
  per cell (10k+ closures on multi-year views), detached in `destroy()`, plus a
  `handleResize` that re-pins the scroll to the end only when the user was
  already at the end (tracked on scroll — measuring inside the resize handler
  reads post-resize geometry and never re-pins). Tag cloud/timeline:
  signature-based no-op skip, with in-place size updates when only tag
  frequencies changed. `ColumnConfigService` memoizes config reads per render
  cycle (invalidated at cycle start and on writes). Charts: line/area drop point
  markers above 500 points per dataset (hover/hit radius preserved), dense
  scatter shrinks its dots.
- **#135** — the grid lays out one skeleton per _expected card_ on the first
  frame (a property can hold several visualizations, or none when hidden by an
  overlay); each render batch promotes its real cards in front of the remaining
  skeletons and retires one skeleton per card. Replaced the dimming spinner
  overlay (which hid exactly this progress) with the skeletons; the height
  reservation stays.
- **#123** — pinning: `pinnedCards` per view (same serialized form as
  `manualOrder`), applied after order reconciliation so pins always win. Star
  button in every card header (property + overlay) plus a context-menu entry;
  `computeEffectiveOrder` gained an optional `pinnedCards` input.

## Decisions (batch)

- **Chart.js decimation deliberately not enabled** (#104 suggested it): the
  plugin requires a `linear`/`time` x scale with `parsing: false`, and these
  charts use a category scale of formatted period labels — it would silently do
  nothing. Marker suppression gives the actual win without dropping data points,
  which matters for tracked data where a single day is meaningful. Migrating the
  cartesian x axis to a time scale is a possible follow-up.
- The **star is always the same outline icon**; CSS fills it gold when pinned.
  Same shape in both states, so the action row's layout is identical on every
  card (visual stability rule).
- **`common-value` buttons** from #125 were left out: bounded properties already
  get one-tap entry from the slider, and the issue's stated use cases (water,
  pushups) are unbounded counters.

## Adversarial review (codex, gpt-5.6-sol high) — findings fixed

Two rounds (review, then verify-the-fixes). Round 1: ten findings, all real:

- **NUL byte in source**: the tag-cloud signature separator was a literal `\0`
  (and then a literal `\x1f`), which made git treat the `.ts` file as binary.
  Now an escaped `'\u001f'` constant; added a repo-wide control-character check.
- **Tag-cloud signature ignored `maxFrequency`**: font sizes are relative to the
  overall max, so a frequency change outside the rendered slice left visible
  tags at the wrong size. Included in the signature.
- **`handleResize` measured after the resize**: see #104 above.
- **Skeleton count assumed one card per item**: see #135 above.
- **Roving tabindex reset on in-place heatmap updates**: the Tab stop is now
  preserved where the user left it.
- **Stale pins written back**: `toggleCardPin` now reconciles against the
  current effective order before persisting.
- **Default-value wrapper clipped the stepper** (`max-width: 180px` +
  `overflow: hidden`, and `w-full` on the mobile input): the wrapper gets a
  marker class that lifts the cap, step buttons are `flex-shrink-0`, and the
  input flexes inside a stepper on mobile.
- **Docs overstated pinning**: pins are per property (a property's cards render
  together) and the context-menu entry exists for property cards only —
  corrected in `docs/usage.md` and the business rules.
- **`format:check` failed** on two docs edited after the last format run.

Round 2 (verifying those fixes) found five more, also fixed:

- **Heatmap empty-data early return kept the old grid's references**, so a later
  in-place update patched a detached grid and the card stayed stuck on the empty
  state. Extracted `releaseRenderedGrid()` and call it on both render paths and
  in `destroy()`.
- **The single-card rebuild path never wired the pin control**, so any config
  change (colour, scale, aggregation…) made the star vanish until the next full
  render. Now wired like the full path.
- **The `max-width: 180px` cap still won on specificity** over the stepper
  marker class, and the mobile `order: 2` applied to steppers nested in the
  "Use default" row, putting the button before the −/input/+ group. Both
  selectors tightened.
- **README still said "right-click any card" to pin** — overlay right-click
  opens the overlay editor. Reworded.
- **A stray control character and a Prettier violation** in this history file.

Round 3 (verifying round 2) found five more, also fixed:

- **Heatmap live→empty could take the in-place path**: empty data reports today
  as its min/max, which can satisfy `canUpdateInPlace()`, leaving a grid of blank
  cells instead of the empty state. `update()` now re-renders whenever the new
  data has no cells.
- **Mobile stepper rendered −, +, input**: the generic mobile rule gives
  `.lt-editor-input--number` `order: 2`, which also applied inside the stepper.
  Reset to `order: 0` there.
- **Popout windows**: `target instanceof Element` is false for nodes from
  another window realm, which would have made heatmap delegation inert and every
  control swipeable in a popout. Added `getEventElement()` (duck-types `closest`)
  and used it in both places.
- **Tooltip leak**: tag cloud and timeline built a new body-level tooltip on
  every structural render without destroying the previous one.
- **Pinning docs still overstated**: unconfigured cards and empty-state cards
  have no star (no header is rendered); documented precisely.

## Live-testing fixes (pinning)

First hands-on test found the star doing nothing. Cause: `toggleCardPin` (and
`resetCardOrder`, same latent bug) cleared `previousOrderSignature` to "force" a
full render — but `canDoIncrementalUpdate()` reads a null signature as "nothing
to compare" and _allows_ the incremental path, so the DOM order never changed
and the card was never re-rendered (so the star's state never updated either).
Both now set `forceFullRender`, the flag `rebuildView()` actually consumes.

The star also flips optimistically on click, so the control answers immediately
instead of waiting for the debounced rebuild.

Icon: always the outline `star`; `.lt-pin-btn--pinned` fills it with
`var(--color-yellow)`. The crossed-out `star-off` is gone, and the context menu
uses the same star.

## Open questions (batch)

- Pins vs. drag order: dragging a pinned card below unpinned cards does not
  unpin it (documented). Consider auto-unpinning on drag if it feels wrong live.
- Needs manual runtime verification: skeleton→card swap order in a 3-column
  grid, swipe feel on a phone, −/+ commit behavior, pin persistence per view.

## Custom heatmap color schemes (#82) + colorblind-friendly palettes (#136)

Heatmaps could only shade one hue by intensity, which makes mood-style 1..5
scales unreadable (2 vs 3 is a shade). They can now map specific values to
specific colors, and both heatmaps and charts gained CVD-safe palettes.

- `HeatmapColorScheme` is a discriminated union: `{ kind: 'gradient', empty,
levels[5] }` (unchanged behavior) or `{ kind: 'discrete', empty, mapping,
fallback? }`. Discrete mode ignores min/max entirely.
- `color.utils.ts` gained `resolveHeatmapCellColor` (the single entry point
  renderers use), `isDiscreteHeatmapScheme`, `normalizeHeatmapColorScheme`,
  `asChartColorScheme`, `createDefaultDiscreteScheme`, `nextDiscreteEntryColor`.
  `getHeatmapColor`/`applyHeatmapColorScheme` narrowed to gradient schemes.
- New `applyCellColor()` in the renderer, used by **all five** granularity paths
  _and_ the in-place update path — the plan in the issue only covered the daily
  one, and the other four would have silently kept ignoring discrete schemes.
- The legend follows the scheme: "Less → More" for gradients, one labelled
  swatch per entry for discrete (unreadable otherwise).
- Storage: `colorScheme` widened to `StoredColorScheme = ChartColorScheme |
HeatmapPresetName | HeatmapColorScheme`. Charts narrow it with
  `asChartColorScheme()`, so a heatmap's inline object or a heatmap-only preset
  name resolves to the default chart palette instead of leaking into a
  `ChartConfig`.
- UI: heatmaps get their own "Colors" dropdown in the card popover (gradients +
  "Custom mapping…" + "Default"), with a mapping editor modal (value input,
  color picker, remove, add entry, fallback color). The settings preset dropdown
  is now type-aware too.
- #136: `colorblind` chart scheme (Okabe-Ito 8 colors), `viridis` and `cividis`
  heatmap gradients, and new mappings seed from a CVD-safe blue → orange ramp
  with added entries cycling Okabe-Ito.

## Decisions (#82/#136)

- **No settings migration.** The issue's plan called for normalizing legacy
  `{ empty, levels }` objects on load, but no such object has ever been
  persisted — presets are code constants and view config stores names. Instead
  `normalizeHeatmapColorScheme()` validates at point of use (view config comes
  from a `.base` file, so it can be anything) and upgrades the bare shape for
  free.
- **Heatmaps and charts no longer share one options list.** They were coupled by
  coincidence (green/blue/purple/orange/red exist in both); adding viridis and
  the Okabe-Ito scheme made the overlap accidental rather than meaningful.
- **Inline background only for discrete cells.** Gradient rendering keeps its
  CSS variables and level classes — no churn, and no `!important` needed since
  inline styles already win. The in-place update path must _clear_ the inline
  background when the scheme is not discrete, or a gradient switch would keep
  the old color.
- **Accessible by default, not by opt-in** (#136): a fresh custom mapping is
  already colorblind-safe, and deliberately avoids red → green — the axis most
  deficiencies collapse — as its default ramp.
- **Modal closes the popover first**, like the scale modal (the reference-line
  modal stacks instead, which means two focus traps and two Escape handlers).

## Open questions (#82/#136)

- Range mappings (`1-2 → red`) deferred until asked for; exact values cover the
  issue as filed.
- Presets (settings tab) store scheme _names_ only — a custom mapping is
  per-card. Worth revisiting if users ask for "all mood properties use this
  mapping".
- **Needs manual runtime verification in a vault**: the mapping editor (add /
  remove / fallback / cancel-discards), discrete cells actually painting per the
  mapping, the discrete legend staying pinned at the card bottom without
  breaking visual stability across cards, switching a card discrete → gradient →
  discrete, and the new viridis/cividis/colorblind entries in all three
  dropdowns (view options, card popover, settings presets).

## Value polarity (#21) + value/range emojis (#22)

Shipped together: #21 is what makes #22's defaults meaningful, and #21 names #22
as its payoff ("that way, we can map emojis or other fun things").

- `polarity` on `PropertyDefinition`: `neutral` (default) | `higher-is-better` |
  `lower-is-better`. Optional field, absent reads as neutral — definitions from
  older versions and from the Starter Kit plugin keep working untouched.
- **Trend arrow** is the behavior the business rules explicitly deferred to #21
  ("the arrow color stays neutral… see issue #21"). It now colors green/red by
  sentiment and words it as improving/worsening. A flat trend and a neutral
  property both stay muted.
- **Default heatmap gradient** by polarity (green / red), as the _lowest_
  precedence step: per-card scheme → view-wide setting → polarity → green.
- `valueEmojis` on `PropertyDefinition`: value or range → emoji. New
  `emoji-mapping.utils.ts` parses keys (`3`, `1-2`, `1..2`, `-5--1`, reversed
  ranges normalized) and resolves values, exact keys beating ranges.
- Emojis render in heatmap and chart tooltips, and as one-tap buttons under the
  number editor — the "editing through emojis" half of the issue. Not in compact
  grid cells (visual stability).
- New settings editors: a "Value direction" dropdown and a value-emoji list,
  offered only for properties whose values are numeric (number, checkbox, or
  text with a value mapping).

## Decisions (#21/#22)

- **Neutral is the default and means silence.** The plugin must not guess
  whether a rising number is good news; every polarity-driven behavior is opt-in.
- **Color is never the only channel.** The arrow glyph and the wording carry the
  same meaning as the green/red — consistent with the #136 work earlier today.
- **Chart palettes are not polarity-driven.** A series color identifies the
  series, not its goodness; only the trend indicator carries sentiment.
- **Overlay charts stay neutral** — several properties, no single polarity — so
  `getVisualizationConfig` simply gets no definition for them.
- **Ranges are in scope for #22** (they were deferred in #82) because the issue
  asks for "values or ranges" by name.
- **`valueEmojis` is a separate field from `valueMapping`**, not an extension of
  it: they run in opposite directions (text → number for input vs number → emoji
  for display) and a property can legitimately use both.
- CSS: the trend sentiment class lands on the trend row's first child, which
  `.lt-chart-trend-item:first-child` (0,2,0) would otherwise win over — resolved
  with the documented doubled-class idiom rather than `!important`.

## Open questions (#21/#22)

- **Polarity-derived heatmap color may be inert**: it only applies when
  `getStringConfig(getConfig, 'heatmapColorScheme')` returns undefined. If
  Obsidian materializes a declared view-option `default` into stored config, that
  read returns `'green'` even when untouched and polarity never gets a turn. The
  trend arrow is unaffected (it does not read view config). **Verify in a vault.**
- Emoji quick-entry is wired into the number editor only. Checkbox properties
  can have a polarity but get no emoji buttons yet.
- **Needs manual runtime verification**: the trend arrow coloring and wording for
  both polarities, the emoji row in the capture modal (selection state, range
  buttons recording the lower bound, 44px tap targets on mobile), the settings
  editors (invalid-key marker, add/remove, the blur-then-rerender dance the
  existing value-mapping editor uses), and emoji prefixes in both tooltip kinds.

## Adversarial review (codex, gpt-5.6-sol high) — #21/#22

Two rounds. Round 1: eight findings, all verified real before fixing.

- **Config-only definition changes were swallowed** (regression this work
  introduced). `property-definitions-changed` cleared the cache and called
  `onDataUpdated()`, which takes the _incremental_ path when the data signature
  is unchanged — fine while definitions only carried `valueMapping` (which
  changes the data), but polarity and emojis are config, so nothing updated
  until reload. Now sets `forceFullRender`, like the preset path already did.
- **Emoji key editing orphaned entries**: the row's handlers used the
  render-time key, so typing `10` into a blank row wrote `1` _and_ `10`, and the
  emoji field then wrote to a key that no longer existed. Rows now track their
  current key/emoji. The existing `valueMapping` editor has the same latent bug
  — left alone, but worth a follow-up issue.
- **Renaming onto an existing key silently destroyed it** — now rejected and
  marked invalid.
- **Copying a property definition dropped both new fields** (the copy is built
  field by field).
- **Range buttons highlighted only at their lower bound**, and overlapping
  ranges could light up two buttons. Extracted `findEmojiEntry` as the single
  precedence source, shared by tooltips and the editor's selection state.
- **Bubble tooltips had no emoji** while the docs claimed all chart points —
  added, and the docs now name the covered chart types.
- **`.lt-input-invalid` (0,1,0) lost** to Obsidian's `input[type='text']`
  rules (0,1,1) — doubled-class idiom, as the CSS rules require.
- **Exact keys overflowing to `Infinity`** were accepted and could never match.

Round 2 (verifying those fixes) found three more:

- **The duplicate check read the render-time snapshot**, so sibling rows renamed
  since the last render were invisible: `1→3` then `2→3` still collided, and
  `1→3→1` reported a phantom duplicate. Now reads live settings.
- **`forceFullRender` in the `full` catch-all was over-broad** — that branch
  serves every untyped `updateSettings` call, and forcing a rebuild there is a
  heavy default. Reverted; instead the polarity and emoji editors now announce
  `{ type: 'property-definitions-changed' }`, which is the branch that forces it.
  (The reviewer also claimed confetti/animation changes route through `full` —
  they do not, they have their own branches.)
- **Per-keystroke `updateSettings` calls can persist out of order** — two rapid
  edits both start `saveData`, and the later write can land first. **Not fixed
  here**: it is pre-existing and plugin-wide (every settings text field has this
  shape, including the `valueMapping` editor this one was modelled on), and
  fixing it properly means serializing writes in `saveSettings`, which is a core
  change well outside these two issues. Worth its own issue.

## Follow-ups from the #21/#22 review — now fixed

The three items deferred out of that review have been closed.

- **Settings writes are serialized.** New `src/utils/serial-queue.utils.ts`
  (`createSerialQueue`), used by `plugin.saveSettings()`. Editors call
  `updateSettings` per keystroke, so two `saveData` calls could be in flight at
  once and land out of order, persisting the _older_ snapshot — visible only
  after a restart. Each queued write persists the settings as they are when it
  runs, so the last write always wins; a failed write rejects for its own caller
  without wedging the queue. Extracted as a utility rather than inlined so it is
  actually testable (the plugin class needs the Obsidian runtime).
- **The `valueMapping` editor had the same orphaned-key bug** as the emoji
  editor: typing `ok` into a fresh row left an orphaned `o` entry, and the number
  field then wrote to a key that no longer existed. Same fix — track the current
  key, reject renames onto an existing key, read live settings for the duplicate
  check. It now also announces `property-definitions-changed`.
- **The polarity-derived heatmap default is no longer a coin flip.** It used to
  depend on `getStringConfig('heatmapColorScheme')` returning undefined, which
  fails if Obsidian materializes a declared view-option `default` into stored
  config. The view option now defaults to an explicit `auto` sentinel
  (`HEATMAP_SCHEME_AUTO`, labelled "Automatic (by value direction)"), which the
  resolver reads as "nothing chosen". Deterministic either way, and it gives
  users a visible control instead of hidden behavior.

### Decisions

- **`auto` as a sentinel beats guessing.** Trying to infer "did the user touch
  this?" from a value that equals the default is unknowable by construction;
  adding a value that _means_ untouched removes the question. Existing views that
  stored `green` keep green — that reads as a deliberate choice, which it was.
- **The serial queue is a utility, not a method.** `saveSettings` lives on a
  class extending Obsidian's `Plugin`, which cannot be instantiated under
  `bun test`; the queue's ordering guarantees are exactly what needs proving.
- Per-keystroke edits are safe for render cost: `onDataUpdated` is debounced, so
  the forced full render collapses to one rebuild.

## Obsidian Starter Kit integration — phase 1

Life Tracker can now import property definitions from the Obsidian Starter Kit
plugin instead of making users define them twice. Entirely optional and additive:
without Starter Kit nothing changes at all.

Investigated the sibling repo first (`../obsidian-starter-kit-plugin`). It
exposes a real `plugin.api` (`PluginApi`) with `listNoteTypes`,
`listGlobalProperties`, `getNoteType(ByName)`, `recognizeNoteType(path)`, each
returning an `ApiResult<T>` envelope and **deep copies**. The two plugins were
already built to fit — `PropertyDefinition`, `ObsidianPropertyType`, `Mapping`,
`NumberRange` all say "Compatible with Obsidian Starter Kit plugin's …".

- New `starter-kit.types.ts`: Life Tracker's own copies of the shapes it reads
  (the plugins share no build), plus `StarterKitPropertyLink`.
- New `starter-kit.utils.ts` (pure, 31 specs): `applyStarterKitStructure`,
  `planImport`, `applyImportPlan`, `syncLinkedDefinitions`, `findOrphanedLinks`.
- New `starter-kit.service.ts`: thin and defensive — locates the plugin,
  feature-detects every API method, unwraps the envelope, catches throws.
- `PropertyDefinition.starterKitLink`; resync runs on `onLayoutReady` (not
  `onload`, where Starter Kit may not be registered yet) and writes nothing when
  the structure already matches.
- New "Starter Kit" settings tab: detection status, grouped import list with
  per-property actions, linked-properties list with unlink. Origin badge on
  linked definitions in the main property list.

## Decisions (Starter Kit)

- **Ownership split: Starter Kit owns structure, Life Tracker owns presentation
  and value semantics.** A linked definition re-reads name/type/constraints/
  defaults/mappings; `polarity`, `valueEmojis`, `valueMapping`, `order` and `id`
  are never touched by a sync. That is the whole reason to link rather than copy.
- **Did not mirror `polarity` / `valueEmojis` into Starter Kit's schema**, even
  though we own that plugin too and were invited to. Starter Kit would store them
  without ever acting on them, and two homes for one value is exactly the drift
  linking exists to prevent. Promote them only if Starter Kit grows a use.
- **Mappings are copied, never re-derived.** Starter Kit's recognition has rules
  that are easy to get wrong: several note types can match one note and **tag
  matches take priority**. Worth noting its `NoteType.mappings` doc says "ALL must
  match" while `getMatchResult` returns on the _first_ match — the doc is wrong,
  the code is OR. Life Tracker asks a different question ("does this property
  apply to this note?"), which is a plain OR, so copying is correct and deciding
  _which_ type a note is stays Starter Kit's job.
- **Property name is the import identity**, since two definitions for one
  frontmatter key would both write to it. A name held by a definition linked to a
  different source is a conflict and is skipped — silently rebinding would be the
  most destructive thing this integration could do.
- **Nothing is ever deleted because Starter Kit went away.** A link whose source
  vanished keeps its last-synced structure and is flagged; unlinking costs the
  user nothing.
- The plugin id `obsidian-starter-kit` is hardcoded: that plugin is deliberately
  not going into the community catalog, so the "id must not contain obsidian"
  rule will never force a rename.

## Adversarial review (codex, gpt-5.6-sol high) — Starter Kit

Nine findings, all verified against both codebases before fixing. Two were
genuinely dangerous:

- **Scope inversion (data hazard).** A Starter Kit note type whose mappings are
  all disabled recognizes _nothing_. Life Tracker reads an empty mapping list as
  "applies to **every** note" (`propertyApplies` returns true when nothing is
  enabled), so importing such a property would have flipped its scope from
  nothing to everything — writing frontmatter into unrelated notes. There is no
  way to express "matches nothing" in a definition, so `isImportableSource`
  refuses them, and sync refuses to widen an already-linked one.
- **Duplicate names within Starter Kit.** `planImport` compared every source
  against the _original_ definitions only, so two note types both defining
  `mood` planned as two `create`s (two definitions writing one frontmatter key)
  or two `adopt`s (the second silently rebinding the first's link). The plan now
  tracks names it has already claimed.

The rest:

- Blank Starter Kit property names were importable.
- Starter Kit has property types Life Tracker lacks (`select`, `url`, `time`,
  plus legacy `multitext`/`boolean`); they were stored verbatim, leaving an
  invalid type. `normalizeStarterKitType` folds them onto Life Tracker's set.
- The service validated only the `ApiResult` envelope, not that `data` was an
  array of well-formed note types — a changed API could throw into a settings
  render, and the startup sync's `void` call would become an unhandled
  rejection. Both hardened.
- **The Import button never enabled**: its disabled state was captured at render
  and the toggle deliberately skips re-rendering, so the tab was unusable. The
  button is now updated in place.
- New definitions took `order = list.length`, which reuses an order once
  deletions leave gaps; now `max(order) + 1`.
- The README claimed links "refresh automatically". Sync runs once on
  `onLayoutReady`; added a **Refresh** button and corrected the wording.
- Renaming a property in Starter Kit breaks the link, because the name _is_ the
  join key and Starter Kit properties have no stable id. Documented rather than
  worked around — there is no identifier to follow.

Also noted and deliberately left alone: Life Tracker's mapping matching is
case-insensitive where Starter Kit's is case-sensitive, so a linked property can
apply to marginally more notes. Changing shared matching semantics for every
user to serve this integration would be the worse trade.

## Starter Kit — stable property ids (both plugins)

Closed the rename limitation flagged above, which needed a change on the Starter
Kit side first.

**In `obsidian-starter-kit-plugin`** (committed there): `PropertyDefinition`
gained an `id` (`prop-<timestamp>-<random>`), minted once and never changed —
including across renames. `backfillPropertyIds` assigns them on load for
definitions written before the field existed; every creation path (plugin API,
note type modal, settings tab) mints one; update paths mutate in place so a
rename keeps it. Verified it does not touch md-base output, so that repo's
Cross-Implementation Serialization Parity Rule is unaffected — `cargo test` (301)
and `bun test` (532) both pass. Its Rust CLI needed no change: the settings
deserializer has no `deny_unknown_fields`.

**Here**: `StarterKitPropertyLink.propertyId` is now the preferred join key,
with the name kept as fallback and as the human-readable identification when
Starter Kit is not installed. `linkMatches` prefers the id when both sides have
one; the note type must still agree either way.

`planImport` now resolves **by link before by name**. That was a real follow-on
bug: after a rename the definition still carries the old name until a sync runs,
so a name-only lookup would have found nothing and planned a duplicate `create`
— two definitions writing the same frontmatter key.

### Review round (codex, gpt-5.6-sol high) — three more, all real

- **A rename could create two definitions for one frontmatter key.** `mood` →
  `feeling` while an unrelated definition already used `feeling` produced two
  `feeling` definitions, via both `planImport` and `syncLinkedDefinitions`. Both
  now refuse the rename and report a conflict.
- **`claimedNames` blocked the genuine linked source.** Names were claimed even
  by entries that ended up conflicts, and `byName` never released the old name of
  a definition that was itself being renamed — so a new Starter Kit property
  taking a vacated name was falsely conflicted. `planImport` was rewritten:
  link-resolution first, old names released, names reserved for link-resolved
  sources, and only successful entries claim.
- **The mixed-version guarantee was overstated in the docs.** See below.

### Decisions

- **The id is recorded on the link only when Starter Kit supplies one**, so the
  field's absence keeps meaning "fall back to the name" rather than "no match".
- **The name fallback has one hole, documented rather than papered over**: a link
  with no id yet can only match by name, so if its property is renamed _and_
  another takes the old name before the first sync, it binds to the newcomer.
  One sync records the id and closes the window. Unfixable while either side
  lacks an id — the two properties are indistinguishable by the only key there
  is, and dropping the fallback would orphan every pre-id link, which is worse.
- **Mixed states resolve by name**: a link made before ids existed against a
  Starter Kit that has since backfilled them still matches, so upgrading either
  plugin in either order never orphans anything.
- Life Tracker did **not** gain a duplicate of Starter Kit's id generator — ids
  are minted there and only ever read here.

## Open questions (Starter Kit)

- The rename limitation noted earlier is closed (see above).
- **Phase 2/3 not started**: note-type-aware views via `recognizeNoteType` and
  grouping by note type. Filtering stays with Bases, which already does it.
- `recognizeNoteType` is async and per-path; a batch API on the Starter Kit side
  would be worth adding before phase 3 rather than awaiting it per entry.
- Life Tracker only reads. Writing back (e.g. creating a note type property from
  here) is out of scope.
- **Needs manual runtime verification**: detection with the plugin present and
  absent, the import list grouping and conflict rows, that importing preserves
  polarity/emojis on an adopted definition, the orphaned-link state after
  renaming a note type in Starter Kit, and that a normal startup with links
  present writes nothing.

## Starter Kit integration — validated against a real vault

First validation against `~/notesSeb` rather than fixtures. It changed the
design.

**Disclosure**: the Starter Kit repo's `bun run build` installs into that vault
and hot-reload picked it up, so the id backfill ran against the live
`data.json` — 524 ids written, all unique and well-formed, purely additive. That
was an unintended side effect of building, not a deliberate migration. The vault
also now runs a dev build of Starter Kit rather than the released 1.4.0.

### What the real data showed

- 49 note types, 524 properties, but only **284 distinct names** — 91 names
  appear in more than one note type (`rating` in 22, `status` in 16). Life
  Tracker has one definition per frontmatter key, so those genuinely collide:
  the plan reports 240 conflicts, which is correct but had to be made legible.
- All **92** existing Life Tracker definitions correspond to Starter Kit's
  "Daily Notes" — zero Life-Tracker-only, zero multi-note-type ambiguity. The
  `adopt` path covers this vault completely.
- **The ownership split was wrong about `displayName`.** Adopting would have
  renamed 90 labelled properties ("Energy Level" → `health_energy_level`)
  because Starter Kit defaults that field to the raw name. It is presentation,
  which Life Tracker owns by the plan's own principle. Fixed; a dry run over the
  real data now reports **0 label changes** and **0 type changes** (the type
  normalizer already handles Starter Kit's `time`/`select`).
- Adoption still adjusts 47 number ranges (1-10 → 0-10) and 10 required flags.
  Those are structure and Starter Kit is the source of truth, so they are
  applied — but the banner says so before the user commits.

### Version skew, simulated on the real data

Emulating the released Starter Kit by stripping ids: 92 adopt and link by name
with no `propertyId`. Starter Kit then updates and gains ids → one sync upgrades
all 92 links to carry `propertyId` → the next sync is a **no-op**, so startup
does not rewrite settings forever. Both upgrade orders are safe.

### UI changes this forced

- Note type groups are **collapsed by default** with counts (`12 properties · 3
linked · 2 unavailable`); only an expanded group builds rows. 524 rows up
  front was unusable.
- A conflict now names the note type that already holds the contested name.
- A one-click banner links every already-matching property, which is the
  seamless path for a vault configured before this integration existed.

### Still to verify manually

- That the Starter Kit id backfill writes **once** rather than on every start
  (the second-sync no-op is proven for Life Tracker, not for Starter Kit's own
  backfill).
- The collapsed group UI and the adopt banner in a live settings tab.

## Releases and release tooling (issue #117)

Cut **Starter Kit 1.5.0** and **Life Tracker 2.20.0**, and installed both into
`~/notesSeb` from the published release artifacts (sha256-verified against the
assets; neither plugin's `data.json` was touched).

Fixed the live parts of #117 first, so the Life Tracker release would not repeat
the bug:

- **`versions.json` was only appended when a release introduced a _new_
  `minAppVersion`.** That minimum changes rarely, so the first release using one
  recorded itself and every later release sharing it was silently skipped —
  which is why the file sat at `0.1.0` and `2.7.4` while 2.7.5 through 2.19.0
  shipped unrecorded. Now every release gets an entry, matching Obsidian's own
  sample plugin. Verified: 2.20.0 appears in `versions.json` after the release.
- **The release gate ran `build` and `test` but not `tsc` or `lint`**, so a
  commit failing either could still be tagged and published. Both now run first.
- The third bullet, double-trigger, **was already handled**: a tag pushed with
  `GITHUB_TOKEN` does not fire the tag-push event, the publish job is dispatched
  explicitly, and the two jobs have mutually exclusive `if:` conditions plus a
  `concurrency` group.

The rule moved into a pure `recordVersion()` so it is testable — the old spec
only exercised type shapes, which is exactly how the bug survived.

Nothing is backfilled, and nothing should be: sparse _is_ the correct shape.

**Starter Kit does not have a bug here either** — I claimed it did, on the same
mistaken reading. Its `scripts/version-bump.ts` uses the same conditional and is
fine as it stands.
