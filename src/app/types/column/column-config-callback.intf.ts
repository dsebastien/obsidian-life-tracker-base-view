import type { ColumnConfigResult } from './column-config-result.intf'

/**
 * Callback when user completes column configuration
 */
export type ColumnConfigCallback = (result: ColumnConfigResult) => void
