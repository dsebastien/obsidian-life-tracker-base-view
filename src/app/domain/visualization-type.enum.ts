/**
 * Visualization type for rendering data
 */
export enum VisualizationType {
    Heatmap = 'heatmap',
    LineChart = 'line-chart',
    BarChart = 'bar-chart',
    TagCloud = 'tag-cloud',
    Timeline = 'timeline'
}
